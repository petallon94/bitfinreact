import React from 'react';

const PostCommendBox = () => {

  //댓글더보기 누르면 토글기능 !prev하면 false는 true로, true는 false로 변경됨 
// const onToggleComment = useCallback(() => {
//   setCommendFormOpened((prev) => !prev);
//   },[]);

        return (
           <div>
               댓글창
          </div>
/*   {commentFormOpend && (
          <div>
               댓글부분
             </div>)}
            <ConmmentForm/> 
           <Comments/> 
           </div>*/

        )
    }

export default PostCommendBox;